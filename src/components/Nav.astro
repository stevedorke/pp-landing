---
const base = import.meta.env.BASE_URL;
---

<nav class="nav">
  <div class="nav-inner">
    <a href={base} class="brand">
      <span class="brand-icon">🧠</span>
      <span class="brand-name">Pocket Personality</span>
    </a>
    <div class="nav-links">
      <a href={`${base}#features`}>Features</a>
      <a href={`${base}privacy`}>Privacy</a>
      <a href={`${base}terms`}>Terms</a>
    </div>
  </div>
</nav>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(15, 10, 60, 0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255,255,255,0.07);
  }
  .nav-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 24px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    font-size: 1.05rem;
    color: #fff;
  }
  .brand-icon { font-size: 1.3rem; }
  .nav-links {
    display: flex;
    gap: 28px;
    font-size: 0.92rem;
    color: rgba(255,255,255,0.75);
  }
  .nav-links a:hover { color: #fff; }
</style>
